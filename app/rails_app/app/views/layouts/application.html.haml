!!! XML
!!!

%html{html_attrs}
  %head
    = stylesheet_link_tag 'screen', :cache => true

  %body
    - if flash.any?
      #flash
        - flash.each do |type, message|
          %div{:class => type}
            = message

    #main
      = yield
