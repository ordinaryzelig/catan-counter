.player_counts
  .victory_points= pluralize(player.victory_points, 'victory point')
  - if @game.uses?(CitiesAndKnights)
    .knights
      = pluralize(player.knights.activated.size, 'activated knight')
